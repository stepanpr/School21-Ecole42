server {
	listen 5000				default;
	listen [::]:5000		default;
	root 					/www;
	index					index.php;
	server_name				_;
	location ~ \.php$ {
        fastcgi_pass      127.0.0.1:9000;
        include           fastcgi.conf;
    }
}